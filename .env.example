# =============================================================================
# Local Environment Template
# Copy this file to .env.local and replace with your actual Supabase project
# =============================================================================

# Your Supabase Project (replaces demo values in .env.development)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"
NEXT_PUBLIC_SUPABASE_URL="https://[YOUR-PROJECT-REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Required: Generate with `openssl rand -base64 32`
NEXTAUTH_SECRET="your-secret-here"

# Required: Your Supabase service role key (keep secret)
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Optional: OAuth providers
GITHUB_ID="your-github-oauth-id"
GITHUB_SECRET="your-github-oauth-secret"

# Optional: MCP services
CONTEXT7_API_KEY="your-context7-key"
FIGMA_ACCESS_TOKEN="your-figma-token"
